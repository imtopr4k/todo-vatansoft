version: "3.9"

services:
  api:
    build: ./apps/api
    restart: unless-stopped
    env_file:
      - ./.env.api
    environment:
      MONGO_URI: ${MONGO_URI:-mongodb+srv://erenasik1211_db_user:R4RSDeFPjeSXxB10@telegramtodo.gnsvcdl.mongodb.net/TelegramToDo?retryWrites=true&w=majority}
    ports:
      - "8089:8089"

  bot:
    build: ./apps/bot
    restart: unless-stopped
    env_file:
      - ./.env.bot
    environment:
      API_BASE_URL: http://api:8089
    depends_on:
      - api

  web:
    build: ./apps/web
    restart: unless-stopped
    env_file:
      - ./.env.web
    environment:
      VITE_API_BASE_URL: http://92.205.163.182:8089
    ports:
      - "5173:5173"
    depends_on:
      - api
